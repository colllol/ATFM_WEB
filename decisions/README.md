# Decisions

Store accepted ADRs and decision candidates here. Use one file per decision with a stable date or ticket reference. Product choices, contract changes, permissions, schemas, URLs, release policy, and deployment policy belong here rather than only in a ticket.

Suggested states are `Candidate`, `Accepted`, `Rejected`, and `Superseded`.
