# Surface: tools-services-tests

Owner boundary: `tools/`, `services/`, `scripts/`, and `tests/`, including build, packaging, API utility, synchronization, and test harness work.

Generated binaries, packages, credentials, and environment-specific output remain subject to the repository ignore and release rules. Record exact commands and counts for verification.

Cross-surface behavior or release artifacts need a ticket that names the affected consumer and any Owner gate.
