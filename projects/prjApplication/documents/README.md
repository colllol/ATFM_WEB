# Surface: prjApplication

Owner boundary: ASP.NET Web Forms UI, page lifecycle, client scripts, and application configuration that belongs to the web application.

Before editing, read the root `AGENTS.md` and this surface note. Coordinate changes to shared business logic, DTOs, database scripts, or deployment artifacts through their owning surface.

Verification should prefer the existing solution/build scripts, focused page or JavaScript checks, and an HTTP smoke check when IIS or a local host is available.
